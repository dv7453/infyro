# Infyro — Frontend

Web app for Infyro Agent (TanStack Start + Vite). Product overview and full setup live in the [root README](../README.md).

## Local run

```bash
cp .env.example .env
npm install
npm run dev
```

Requires the backend on `VITE_BACKEND_URL` (default `http://localhost:8080`).
